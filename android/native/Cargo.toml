[package]
name = "nexus-android"
version = "1.2.0"
edition = "2021"
rust-version = "1.88"
publish = false

[lib]
name = "nexus_android"
crate-type = ["cdylib"]

[dependencies]
nexus-engine = { path = "../.." }
jni = "0.22.4"
